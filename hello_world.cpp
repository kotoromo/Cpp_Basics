//Same as in C, that is, the header declaration. In this case, we use the header iostream.
#include <iostream>
// Tells the compiler to use the std namespace.
using namespace std;

int main(){
	cout << "Hello World!\n";
	cout << "We can also write the reserved word endl to insert a new-line character." << endl;
	
	return 0;
}